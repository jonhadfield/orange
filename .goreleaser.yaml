version: 2

project_name: orange

before:
  hooks:
    - go mod tidy

builds:
  - id: orange
    main: ./cmd/orange
    binary: orange
    env:
      - CGO_ENABLED=0
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

# Sign and notarize the darwin binaries so macOS Gatekeeper does not refuse a
# tarball downloaded straight from the releases page. Uses goreleaser's
# cross-platform (quill) implementation, so no macOS runner is needed. The
# enabled guard keeps snapshot and fork builds working when the secrets are
# absent; both the certificate and the notary key are required, since signing
# without usable notary credentials would fail the release outright.
notarize:
  macos:
    - enabled: '{{ and (isEnvSet "MACOS_SIGN_P12") (isEnvSet "MACOS_NOTARY_KEY") }}'
      ids:
        - orange
      sign:
        certificate: "{{ .Env.MACOS_SIGN_P12 }}"
        password: "{{ .Env.MACOS_SIGN_PASSWORD }}"
      notarize:
        issuer_id: "{{ .Env.MACOS_NOTARY_ISSUER_ID }}"
        key_id: "{{ .Env.MACOS_NOTARY_KEY_ID }}"
        key: "{{ .Env.MACOS_NOTARY_KEY }}"
        wait: true
        timeout: 20m

archives:
  - name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - LICENSE
      - README.md

checksum:
  name_template: checksums.txt

changelog:
  use: github
  sort: asc

homebrew_casks:
  # The cask token is orange-cli because homebrew-cask already owns the
  # `orange` token (Orange Data Mining, a GUI app). The installed command
  # stays `orange` — that name is uncontested, as their cask ships no binary.
  - name: orange-cli
    binaries: [orange]
    repository:
      owner: jonhadfield
      name: homebrew-tap
      token: "{{ .Env.TAP_GITHUB_TOKEN }}"
    homepage: https://github.com/jonhadfield/orange
    description: Terminal reader for Hacker News
    hooks:
      post:
        # The darwin binaries are signed and notarized, but a notarization
        # ticket cannot be stapled into a bare Mach-O executable, so Gatekeeper
        # reports "Notary Ticket Missing" and blocks first run. Casks set the
        # quarantine flag on download, so clear it here.
        install: |
          if system_command("/usr/bin/xattr", args: ["-h"]).exit_status == 0
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/orange"]
          end
